{%- if section.blocks.size > 0 -%}
<ul id="menu-categories" class="menu">
  {%- for block in section.blocks -%}
  {%- case block.type -%} 
  {%- when 'mega_dropdown' -%}
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-taxonomy menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-{% if block.settings.full_width != blank %}full-width{% else %}sized{% endif %} item-level-0 menu-mega-dropdown item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- assign child_list_handle = block.settings.menu -%}
    {%- if linklists[child_list_handle].links.size > 0 or linklists[child_list_handle].links != blank -%} 
    <div class="sub-menu-dropdown color-scheme-{{block.settings.sub_menu_color}}">
      <div class="container"> 
        <div class="main-content-{{block.id}}">
        <div class="vc_row wpb_row vc_row-fluid vc_row-o-content-top vc_row-flex"> 
          {%- assign counts = linklists[child_list_handle].links.size | plus: 0 -%}  
          {%- case counts -%}
            {%- when 1 -%}  
              {%- assign class_menu = '12' -%}
            {%- when 2 -%} 
              {%- assign class_menu = '6' -%} 
            {%- when 3 -%}  
              {%- assign class_menu = '4' -%}  
            {%- else -%}  
              {%- assign class_menu = '3' -%} 
          {%- endcase -%}
          {%- if counts > 0 -%}
          <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
            <div class="vc_column-inner">
              <div class="wpb_wrapper"> 
                {%- for childlink in linklists[child_list_handle].links limit:1 -%}  
                {%- if block.settings.banner_sub_top1 != blank -%} 
                <div class="spb_single_image spb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                  <figure class="spb_wrapper vc_figure">
                    <div class="vc_single_image-wrapper vc_box_border_grey">
                      <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top1 | img_url: '118x118'}}" width="118">
                    </div>
                  </figure>
                </div>
                {%- endif -%}
                <ul class="sub-menu mega-menu-list">
                  <li>
                    <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                      <span>
                      {% if settings.language_enable %} 
                      <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                      <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                      {% else %}
                      {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                      {% endif %}
                      </span>
                    </a>
                    {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                    {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                    <ul class="sub-sub-menu">
                      {%- if linklists[childlink.handle].links.size > 0 -%}
                      {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                      {%- elsif linklists[third_list_handle].links != blank -%}
                      {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                      {%- endif -%}
                      {%- for thirdlink in menu_sub_sub_link.links -%}
                      {%- if thirdlink.title contains '[' -%}
                      {%- if thirdlink.title contains '[icon_' -%}
                      {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                      {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                      <li>
                        <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <i class="fa fa-{{icon_third}}"></i> 
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                      </li>
                      {%- else -%}
                      {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                      <li class="item-with-label item-label-{{label_third | handleize }}">
                        <a href="{{ thirdlink.url }}"> 
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                          <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                        </a>
                      </li>
                      {%- endif -%}
                      {%- else -%}
                      <li>
                        <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                          <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                          {% else %}
                          {{ thirdlink.title | split: '|' | first }}
                          {% endif %}
                          </span>
                        </a>
                      </li>
                      {%- endif -%}
                      {%- endfor -%}
                    </ul>
                    {%- endif -%}
                  </li>
                </ul>  
                {%- endfor -%}
                {%- if counts > 4 -%}
                  {%- for childlink in linklists[child_list_handle].links offset:4 limit:1 -%}
                    {%- if block.settings.banner_sub_top5 != blank -%} 
                    <div class="spb_single_image spb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                      <figure class="wpb_wrapper vc_figure">
                        <div class="vc_single_image-wrapper vc_box_border_grey">
                          <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top5 | img_url: '118x118'}}" width="118">
                        </div>
                      </figure>
                    </div>
                    {%- endif -%}
                    <ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                    </ul>  
                  {%- endfor -%}
                {%- endif -%}
              </div>
            </div>
          </div> 
            {%- if counts > 1 -%}
            <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
              <div class="vc_column-inner">
                <div class="wpb_wrapper"> 
                  {%- for childlink in linklists[child_list_handle].links offset:1 limit:1 -%}  
                  {%- if block.settings.banner_sub_top2 != blank -%} 
                  <div class="spb_single_image spb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                    <figure class="spb_wrapper vc_figure">
                      <div class="vc_single_image-wrapper vc_box_border_grey">
                        <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top2 | img_url: '118x118'}}" width="118">
                      </div>
                    </figure>
                  </div>
                  {%- endif -%}
                  <ul class="sub-menu mega-menu-list">
                    <li>
                      <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                        <span>
                        {% if settings.language_enable %} 
                        <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                        <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                        {% else %}
                        {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                        {% endif %}
                        </span>
                      </a>
                      {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                      {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                      <ul class="sub-sub-menu">
                        {%- if linklists[childlink.handle].links.size > 0 -%}
                        {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                        {%- elsif linklists[third_list_handle].links != blank -%}
                        {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                        {%- endif -%}
                        {%- for thirdlink in menu_sub_sub_link.links -%}
                        {%- if thirdlink.title contains '[' -%}
                        {%- if thirdlink.title contains '[icon_' -%}
                        {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <i class="fa fa-{{icon_third}}"></i> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- else -%}
                        {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        <li class="item-with-label item-label-{{label_third | handleize }}">
                          <a href="{{ thirdlink.url }}"> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                            <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- else -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- endfor -%}
                      </ul>
                      {%- endif -%}
                    </li>
                  </ul>  
                  {%- endfor -%}
                  {%- if counts > 5 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:5 limit:1 -%} 
                      {%- if block.settings.banner_sub_top6 != blank -%} 
                      <div class="spb_single_image spb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                        <figure class="wpb_wrapper vc_figure">
                          <div class="vc_single_image-wrapper vc_box_border_grey">
                            <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top6 | img_url: '118x118'}}" width="118">
                          </div>
                        </figure>
                      </div>
                      {%- endif -%}
                      <ul class="sub-menu mega-menu-list">
                        <li>
                          <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                          {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                          {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                          <ul class="sub-sub-menu">
                            {%- if linklists[childlink.handle].links.size > 0 -%}
                            {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                            {%- elsif linklists[third_list_handle].links != blank -%}
                            {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                            {%- endif -%}
                            {%- for thirdlink in menu_sub_sub_link.links -%}
                            {%- if thirdlink.title contains '[' -%}
                            {%- if thirdlink.title contains '[icon_' -%}
                            {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <i class="fa fa-{{icon_third}}"></i> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- else -%}
                            {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            <li class="item-with-label item-label-{{label_third | handleize }}">
                              <a href="{{ thirdlink.url }}"> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                                <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- else -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- endfor -%}
                          </ul>
                          {%- endif -%}
                        </li>
                      </ul>  
                    {%- endfor -%}
                  {%- endif -%}
                </div>
              </div>
            </div> 
          	{%- endif -%}
            {%- if counts > 2 -%}
              <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
              <div class="vc_column-inner">
                <div class="wpb_wrapper"> 
                  {%- for childlink in linklists[child_list_handle].links offset:2 limit:1 -%} 
                  {%- if block.settings.banner_sub_top3 != blank -%}
                  <div class="spb_single_image spb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                    <figure class="spb_wrapper vc_figure">
                      <div class="vc_single_image-wrapper vc_box_border_grey">
                        <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top3 | img_url: '118x118'}}" width="118">
                      </div>
                    </figure>
                  </div>
                  {%- endif -%}
                  <ul class="sub-menu mega-menu-list">
                    <li>
                      <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                        <span>
                        {% if settings.language_enable %} 
                        <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                        <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                        {% else %}
                        {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                        {% endif %}
                        </span>
                      </a>
                      {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                      {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                      <ul class="sub-sub-menu">
                        {%- if linklists[childlink.handle].links.size > 0 -%}
                        {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                        {%- elsif linklists[third_list_handle].links != blank -%}
                        {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                        {%- endif -%}
                        {%- for thirdlink in menu_sub_sub_link.links -%}
                        {%- if thirdlink.title contains '[' -%}
                        {%- if thirdlink.title contains '[icon_' -%}
                        {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <i class="fa fa-{{icon_third}}"></i> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- else -%}
                        {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        <li class="item-with-label item-label-{{label_third | handleize }}">
                          <a href="{{ thirdlink.url }}"> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                            <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- else -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- endfor -%}
                      </ul>
                      {%- endif -%}
                    </li>
                  </ul>  
                  {%- endfor -%}
                  {%- if counts > 6 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:6 limit:1 -%}
                      {%- if block.settings.banner_sub_top7 != blank -%}
                      <div class="wpb_single_image wpb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                        <figure class="wpb_wrapper vc_figure">
                          <div class="vc_single_image-wrapper vc_box_border_grey">
                            <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top7 | img_url: '118x118'}}" width="118">
                          </div>
                        </figure>
                      </div>
                      {%- endif -%}
                      <ul class="sub-menu mega-menu-list">
                        <li>
                          <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                          {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                          {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                          <ul class="sub-sub-menu">
                            {%- if linklists[childlink.handle].links.size > 0 -%}
                            {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                            {%- elsif linklists[third_list_handle].links != blank -%}
                            {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                            {%- endif -%}
                            {%- for thirdlink in menu_sub_sub_link.links -%}
                            {%- if thirdlink.title contains '[' -%}
                            {%- if thirdlink.title contains '[icon_' -%}
                            {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <i class="fa fa-{{icon_third}}"></i> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- else -%}
                            {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            <li class="item-with-label item-label-{{label_third | handleize }}">
                              <a href="{{ thirdlink.url }}"> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                                <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- else -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- endfor -%}
                          </ul>
                          {%- endif -%}
                        </li>
                      </ul>  
                    {%- endfor -%}
                  {%- endif -%}
                </div>
              </div>
            </div> 
          	{%- endif -%}
            {%- if counts >= 4 -%}
              <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
                <div class="vc_column-inner">
                  <div class="wpb_wrapper"> 
                    {%- for childlink in linklists[child_list_handle].links offset:3 limit:1 -%} 
                    {%- if block.settings.banner_sub_top4 != blank -%} 
                    <div class="wpb_single_image wpb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                      <figure class="wpb_wrapper vc_figure">
                        <div class="vc_single_image-wrapper vc_box_border_grey">
                          <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top4 | img_url: '118x118'}}" width="118">
                        </div>
                      </figure>
                    </div>
                    {%- endif -%}
                    <ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                    </ul>  
                    {%- endfor -%}
                    {%- if counts > 7 -%}
                      {%- for childlink in linklists[child_list_handle].links offset:7 limit:1 -%} 
                        {%- if block.settings.banner_sub_top8 != blank -%} 
                        <div class="wpb_single_image wpb_content_element vc_align_left vc_custom_1502109599466  rtl-cat-right img-{{forloop.index}}">
                          <figure class="wpb_wrapper vc_figure">
                            <div class="vc_single_image-wrapper vc_box_border_grey">
                              <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top8 | img_url: '118x118'}}" width="118">
                            </div>
                          </figure>
                        </div>
                        {%- endif -%}
                        <ul class="sub-menu mega-menu-list">
                          <li>
                            <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                            {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                            {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                            <ul class="sub-sub-menu">
                              {%- if linklists[childlink.handle].links.size > 0 -%}
                              {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                              {%- elsif linklists[third_list_handle].links != blank -%}
                              {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                              {%- endif -%}
                              {%- for thirdlink in menu_sub_sub_link.links -%}
                              {%- if thirdlink.title contains '[' -%}
                              {%- if thirdlink.title contains '[icon_' -%}
                              {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <i class="fa fa-{{icon_third}}"></i> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- else -%}
                              {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              <li class="item-with-label item-label-{{label_third | handleize }}">
                                <a href="{{ thirdlink.url }}"> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                  <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- else -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- endfor -%}
                            </ul>
                            {%- endif -%}
                          </li>
                        </ul>  
                      {%- endfor -%}
                    {%- endif -%}
                  </div>
                </div>
              </div> 
            {%- endif -%}
          {%- endif -%} 
        </div>
          {%- assign numb = 0 -%}
          {%- for i in (1..4) -%}
            {%- capture brand_image -%}brand_image_{{forloop.index}}{%- endcapture -%}
            {%- if block.settings[brand_image] != blank -%}
          	  {%- assign numb = numb | plus: 1 -%}
            {%- endif -%}
          {%- endfor -%}
          {%- if numb > 0 -%}
        <div class="vc_row wpb_row vc_row-fluid vc_custom_1502091375381 vc_row-has-fill">
          <div class="rtl-cat-footer wpb_column vc_column_container vc_col-sm-12">
          <div class="vc_column-inner vc_custom_1501500011878">
            <div class="wpb_wrapper">
              <div id="brands_{{block.id}}" class="brands-widget slider-brands_{{block.id}} brands-grid brands-hover-alt brands-columns-4 brands-style-default">
                <div class="brands-items-wrapper ">
                  {%- for i in (1..4) -%}
                  {%- capture brand_image -%}brand_image_{{forloop.index}}{%- endcapture -%}
                  {%- capture brand_link -%}brand_link_{{forloop.index}}{%- endcapture -%}
                  {%- if block.settings[brand_image] != blank -%}
                  <div class="brand-item">
                    <a href="{{block.settings[brand_link]}}">
                      <img src="{{block.settings[brand_image] | img_url: '108x' }}" alt="{{block.settings[brand_image].alt}}">
                    </a>
                  </div>
                  {%- endif -%}
                  {%- endfor -%}
                </div>
              </div>
            </div>
          </div>
          </div>
        </div>
          {%- endif -%}
        <style type="text/css" data-type="vc_shortcodes-custom-css">.vc_custom_1502091375381{overflow:hidden;margin-top:5px !important;margin-right:-35px !important;margin-left:-35px !important;background-color:#f9f9f9 !important}.vc_custom_1502109599466{margin-bottom:10px !important}.vc_custom_1502109621671{margin-bottom:10px !important}.vc_custom_1502109617137{margin-bottom:10px !important}.vc_custom_1502109628727{margin-bottom:10px !important}.vc_custom_1501500011878{margin-top:-34px !important;padding-left:0px !important}</style>
        </div> 
      </div>
    </div>
    {%- endif -%} 
    <style type="text/css">
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}
      {%- endif -%} 
      .menu-item-{{block.id}} > .sub-menu-dropdown {
        min-height: 120px; 
        {%- if block.settings.menu_width != blank and block.settings.full_width == blank -%} 
        width: {{ block.settings.menu_width | escape | default: '800' | remove: 'px' | append: 'px' }};
        {%- else -%}
        width: 100%;
        {%- endif -%} 
      }
      .main-content-{{block.id}} { 
        {%- if block.settings.banner != blank and block.settings.show_img -%} 
        background-image: url({{ block.settings.banner| img_url: 'master' }});
        {%- endif -%}
        margin-right: -35px;
        margin-left: -35px;
        padding-left: 35px;
        padding-right: 35px;
        margin-top: -30px;
        padding-top: 30px;
        {{block.settings.banner_custom_style}} 
      };
    </style>
  </li>
  {%- when 'mega_sub_banner_content' -%}
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-taxonomy menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-{% if block.settings.full_width != blank %}full-width{% else %}sized{% endif %} item-level-0 menu-mega-dropdown item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- assign child_list_handle = block.settings.menu -%}
    {%- if linklists[child_list_handle].links.size > 0 or linklists[child_list_handle].links != blank -%} 
    <div class="sub-menu-dropdown color-scheme-dark">
      <div class="container"> 
        <div class="vc_row wpb_row vc_row-fluid svg-color rtl-cat-margin vc_custom_1501503456175 vc_row-o-content-top vc_row-flex"> 
          {%- assign counts = linklists[child_list_handle].links.size | plus: 0 -%}  
          {%- case counts -%}
            {%- when 1 -%}  
              {%- assign class_menu = '6' -%} 
              {%- assign class_banner = '6' -%} 
            {%- else -%}  
              {%- assign class_menu = '4' -%} 
              {%- assign class_banner = '4' -%} 
          {%- endcase -%}
          {%- if counts > 0 -%}
          <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
            <div class="vc_column-inner">
              <div class="spb_wrapper"> 
                {%- for childlink in linklists[child_list_handle].links limit:1 -%}  
                <div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                  {%- assign col_sub = '12'-%}
                  {%- if block.settings.banner_sub_top1 != blank -%} 
                    {%- assign col_thumb = '4'-%}
                    {%- assign col_sub = '8'-%}
                  {%- endif -%}
                  {%- if block.settings.banner_sub_top1 != blank -%} 
                  <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                    <div class="vc_column-inner">
                      <figure class="spb_wrapper">
                        <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                          <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                            <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top1 | img_url: '50x50'}}" width="50">
                          </div>
                        </div>
                      </figure>
                    </div>
                  </div>
                  {%- endif -%}
                  <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                    <div class="vc_column-inner vc_custom_1491550062693">
                      <div class="wpb_wrapper">
                        <ul class="sub-menu mega-menu-list">
                          <li>
                            <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                            {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                            {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                            <ul class="sub-sub-menu">
                              {%- if linklists[childlink.handle].links.size > 0 -%}
                              {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                              {%- elsif linklists[third_list_handle].links != blank -%}
                              {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                              {%- endif -%}
                              {%- for thirdlink in menu_sub_sub_link.links -%}
                              {%- if thirdlink.title contains '[' -%}
                              {%- if thirdlink.title contains '[icon_' -%}
                              {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <i class="fa fa-{{icon_third}}"></i> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- else -%}
                              {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              <li class="item-with-label item-label-{{label_third | handleize }}">
                                <a href="{{ thirdlink.url }}"> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                  <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- else -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- endfor -%}
                            </ul>
                            {%- endif -%}
                          </li>
                        </ul> 
                      </div>
                    </div>
                  </div>
                </div>
                {%- endfor -%}
                {%- if counts > 2 -%}
                  {%- for childlink in linklists[child_list_handle].links offset:2 limit:1 -%}
                	<div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                      {%- assign col_sub = '12'-%}
                      {%- if block.settings.banner_sub_top3 != blank -%} 
                        {%- assign col_thumb = '4'-%}
                        {%- assign col_sub = '8'-%}
                      {%- endif -%}
                      {%- if block.settings.banner_sub_top3 != blank -%} 
                      <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                        <div class="vc_column-inner">
                          <figure class="spb_wrapper">
                            <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                              <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                                <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top3 | img_url: '50x50'}}" width="50">
                              </div>
                            </div>
                          </figure>
                        </div>
                      </div>
                      {%- endif -%}
                      <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                        <div class="vc_column-inner vc_custom_1491550062693">
                          <div class="wpb_wrapper">
                            <ul class="sub-menu mega-menu-list">
                              <li>
                                <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                </a>
                                {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                                {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                                <ul class="sub-sub-menu">
                                  {%- if linklists[childlink.handle].links.size > 0 -%}
                                  {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                                  {%- elsif linklists[third_list_handle].links != blank -%}
                                  {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                                  {%- endif -%}
                                  {%- for thirdlink in menu_sub_sub_link.links -%}
                                  {%- if thirdlink.title contains '[' -%}
                                  {%- if thirdlink.title contains '[icon_' -%}
                                  {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                  {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                                  <li>
                                    <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                      <i class="fa fa-{{icon_third}}"></i> 
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                      {% endif %}
                                      </span>
                                    </a>
                                  </li>
                                  {%- else -%}
                                  {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                  <li class="item-with-label item-label-{{label_third | handleize }}">
                                    <a href="{{ thirdlink.url }}"> 
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                      {% endif %}
                                      </span>
                                      <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                    </a>
                                  </li>
                                  {%- endif -%}
                                  {%- else -%}
                                  <li>
                                    <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first }}
                                      {% endif %}
                                      </span>
                                    </a>
                                  </li>
                                  {%- endif -%}
                                  {%- endfor -%}
                                </ul>
                                {%- endif -%}
                              </li>
                            </ul> 
                          </div>
                        </div>
                      </div>
                    </div>
                  {%- endfor -%}
                {%- endif -%}
                {%- if counts > 4 -%}
                  {%- for childlink in linklists[child_list_handle].links offset:4 limit:1 -%}
                    <div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                      {%- assign col_sub = '12'-%}
                      {%- if block.settings.banner_sub_top5 != blank -%} 
                        {%- assign col_thumb = '4'-%}
                        {%- assign col_sub = '8'-%}
                      {%- endif -%}
                      {%- if block.settings.banner_sub_top5 != blank -%} 
                      <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                        <div class="vc_column-inner">
                          <figure class="spb_wrapper">
                            <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                              <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                                <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top5 | img_url: '50x50'}}" width="50">
                              </div>
                            </div>
                          </figure>
                        </div>
                      </div>
                      {%- endif -%}
                      <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                        <div class="vc_column-inner vc_custom_1491552748079">
                          <div class="wpb_wrapper">
                            <ul class="sub-menu mega-menu-list">
                              <li>
                                <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                </a>
                                {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                                {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                                <ul class="sub-sub-menu">
                                  {%- if linklists[childlink.handle].links.size > 0 -%}
                                  {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                                  {%- elsif linklists[third_list_handle].links != blank -%}
                                  {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                                  {%- endif -%}
                                  {%- for thirdlink in menu_sub_sub_link.links -%}
                                  {%- if thirdlink.title contains '[' -%}
                                  {%- if thirdlink.title contains '[icon_' -%}
                                  {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                  {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                                  <li>
                                    <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                      <i class="fa fa-{{icon_third}}"></i> 
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                      {% endif %}
                                      </span>
                                    </a>
                                  </li>
                                  {%- else -%}
                                  {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                  <li class="item-with-label item-label-{{label_third | handleize }}">
                                    <a href="{{ thirdlink.url }}"> 
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                      {% endif %}
                                      </span>
                                      <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                    </a>
                                  </li>
                                  {%- endif -%}
                                  {%- else -%}
                                  <li>
                                    <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                      <span>
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                      <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                      {% else %}
                                      {{ thirdlink.title | split: '|' | first }}
                                      {% endif %}
                                      </span>
                                    </a>
                                  </li>
                                  {%- endif -%}
                                  {%- endfor -%}
                                </ul>
                                {%- endif -%}
                              </li>
                            </ul> 
                          </div>
                        </div>
                      </div>
                    </div> 
                  {%- endfor -%}
                {%- endif -%}
              </div>
            </div>
          </div> 
            {%- if counts > 1 -%}
            <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
              <div class="vc_column-inner">
                <div class="wpb_wrapper"> 
                  {%- for childlink in linklists[child_list_handle].links offset:1 limit:1 -%}  
                    <div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                    {%- assign col_sub = '12'-%}
                    {%- if block.settings.banner_sub_top2 != blank -%} 
                      {%- assign col_thumb = '4'-%}
                      {%- assign col_sub = '8'-%}
                    {%- endif -%}
                    {%- if block.settings.banner_sub_top2 != blank -%} 
                    <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                      <div class="vc_column-inner">
                        <figure class="spb_wrapper">
                          <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                            <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                              <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top2 | img_url: '50x50'}}" width="50">
                            </div>
                          </div>
                        </figure>
                      </div>
                    </div>
                    {%- endif -%}
                    <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                      <div class="vc_column-inner vc_custom_1491550062693">
                        <div class="wpb_wrapper">
                          <ul class="sub-menu mega-menu-list">
                            <li>
                              <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                              </a>
                              {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                              {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                              <ul class="sub-sub-menu">
                                {%- if linklists[childlink.handle].links.size > 0 -%}
                                {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                                {%- elsif linklists[third_list_handle].links != blank -%}
                                {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                                {%- endif -%}
                                {%- for thirdlink in menu_sub_sub_link.links -%}
                                {%- if thirdlink.title contains '[' -%}
                                {%- if thirdlink.title contains '[icon_' -%}
                                {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                                <li>
                                  <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                    <i class="fa fa-{{icon_third}}"></i> 
                                    <span>
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                    <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                    {% else %}
                                    {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                    {% endif %}
                                    </span>
                                  </a>
                                </li>
                                {%- else -%}
                                {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                <li class="item-with-label item-label-{{label_third | handleize }}">
                                  <a href="{{ thirdlink.url }}"> 
                                    <span>
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                    <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                    {% else %}
                                    {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                    {% endif %}
                                    </span>
                                    <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                  </a>
                                </li>
                                {%- endif -%}
                                {%- else -%}
                                <li>
                                  <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                    <span>
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                    <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                    {% else %}
                                    {{ thirdlink.title | split: '|' | first }}
                                    {% endif %}
                                    </span>
                                  </a>
                                </li>
                                {%- endif -%}
                                {%- endfor -%}
                              </ul>
                              {%- endif -%}
                            </li>
                          </ul> 
                        </div>
                      </div>
                    </div>
                  </div>
                  {%- endfor -%}
                  {%- if counts > 3 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:3 limit:1 -%} 
                      <div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                        {%- assign col_sub = '12'-%}
                        {%- if block.settings.banner_sub_top4 != blank -%} 
                          {%- assign col_thumb = '4'-%}
                          {%- assign col_sub = '8'-%}
                        {%- endif -%}
                        {%- if block.settings.banner_sub_top4 != blank -%} 
                        <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                          <div class="vc_column-inner">
                            <figure class="spb_wrapper">
                              <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                                <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                                  <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top4 | img_url: '50x50'}}" width="50">
                                </div>
                              </div>
                            </figure>
                          </div>
                        </div>
                        {%- endif -%}
                        <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                          <div class="vc_column-inner vc_custom_1491550062693">
                            <div class="wpb_wrapper">
                              <ul class="sub-menu mega-menu-list">
                                <li>
                                  <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                    <span>
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                    <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                    {% else %}
                                    {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                    {% endif %}
                                    </span>
                                  </a>
                                  {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                                  {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                                  <ul class="sub-sub-menu">
                                    {%- if linklists[childlink.handle].links.size > 0 -%}
                                    {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                                    {%- elsif linklists[third_list_handle].links != blank -%}
                                    {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                                    {%- endif -%}
                                    {%- for thirdlink in menu_sub_sub_link.links -%}
                                    {%- if thirdlink.title contains '[' -%}
                                    {%- if thirdlink.title contains '[icon_' -%}
                                    {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                    {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                                    <li>
                                      <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                        <i class="fa fa-{{icon_third}}"></i> 
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                        {% endif %}
                                        </span>
                                      </a>
                                    </li>
                                    {%- else -%}
                                    {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                    <li class="item-with-label item-label-{{label_third | handleize }}">
                                      <a href="{{ thirdlink.url }}"> 
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                        {% endif %}
                                        </span>
                                        <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                      </a>
                                    </li>
                                    {%- endif -%}
                                    {%- else -%}
                                    <li>
                                      <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first }}
                                        {% endif %}
                                        </span>
                                      </a>
                                    </li>
                                    {%- endif -%}
                                    {%- endfor -%}
                                  </ul>
                                  {%- endif -%}
                                </li>
                              </ul> 
                            </div>
                          </div>
                        </div>
                      </div> 
                    {%- endfor -%}
                  {%- endif -%}
                  {%- if counts > 5 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:5 limit:1 -%} 
                      <div class="vc_row spb_row vc_inner vc_row-fluid vc_row-o-content-top vc_row-flex">
                        {%- assign col_sub = '12'-%}
                        {%- if block.settings.banner_sub_top6 != blank -%} 
                          {%- assign col_thumb = '4'-%}
                          {%- assign col_sub = '8'-%}
                        {%- endif -%}
                        {%- if block.settings.banner_sub_top6 != blank -%} 
                        <div class="spb_column vc_column_container vc_col-sm-{{col_thumb}}">
                          <div class="vc_column-inner">
                            <figure class="spb_wrapper">
                              <div class="cursor-pointer woodmart-info-box text-left box-title-default icon-alignment-top box- color-scheme-dark">
                                <div class="info-svg-wrapper info-icon" style="width:50px;height:50px;">
                                  <img class="vc_single_image-img" src="{{ block.settings.banner_sub_top6 | img_url: '50x50'}}" width="50">
                                </div>
                              </div>
                            </figure>
                          </div>
                        </div>
                        {%- endif -%}
                        <div class="wpb_column vc_column_container vc_col-sm-{{col_sub}}">
                          <div class="vc_column-inner vc_custom_1491552748079">
                            <div class="wpb_wrapper">
                              <ul class="sub-menu mega-menu-list">
                                <li>
                                  <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                    <span>
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                    <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                    {% else %}
                                    {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                    {% endif %}
                                    </span>
                                  </a>
                                  {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                                  {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                                  <ul class="sub-sub-menu">
                                    {%- if linklists[childlink.handle].links.size > 0 -%}
                                    {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                                    {%- elsif linklists[third_list_handle].links != blank -%}
                                    {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                                    {%- endif -%}
                                    {%- for thirdlink in menu_sub_sub_link.links -%}
                                    {%- if thirdlink.title contains '[' -%}
                                    {%- if thirdlink.title contains '[icon_' -%}
                                    {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                    {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                                    <li>
                                      <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                        <i class="fa fa-{{icon_third}}"></i> 
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                        {% endif %}
                                        </span>
                                      </a>
                                    </li>
                                    {%- else -%}
                                    {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                                    <li class="item-with-label item-label-{{label_third | handleize }}">
                                      <a href="{{ thirdlink.url }}"> 
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                        {% endif %}
                                        </span>
                                        <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                      </a>
                                    </li>
                                    {%- endif -%}
                                    {%- else -%}
                                    <li>
                                      <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                        <span>
                                        {% if settings.language_enable %} 
                                        <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                        <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                        {% else %}
                                        {{ thirdlink.title | split: '|' | first }}
                                        {% endif %}
                                        </span>
                                      </a>
                                    </li>
                                    {%- endif -%}
                                    {%- endfor -%}
                                  </ul>
                                  {%- endif -%}
                                </li>
                              </ul> 
                            </div>
                          </div>
                        </div>
                      </div> 
                    {%- endfor -%}
                  {%- endif -%}
                </div>
              </div>
            </div> 
          	{%- endif -%}
          {%- endif -%}
          {%- if block.settings.banner != blank -%}
          <div class="rtl-cat-banner wpb_column vc_column_container vc_col-sm-{{class_banner}}">
            <div class="vc_column-inner vc_custom_1501504499570">
              <div class="wpb_wrapper">
                <div class="vc_row wpb_row vc_inner vc_row-fluid vc_custom_1491466272153">
                  <div class="wpb_column vc_column_container vc_col-sm-12">
                    <div class="vc_column-inner vc_custom_1491466426404">
                      <div class="wpb_wrapper">
                        <div class="promo-banner-wrapper" id="banner-id-5a395a0dc1fdf">
                          <div class="promo-banner cursor-pointer banner-vr-align-bottom banner-hr-align-left banner- banner-hover-none color-scheme-dark banner-btn-size-small banner-btn-style-default with-btn banner-btn-position-hover">
                            <div class="main-wrapp-img">
                              <div class="banner-image">
                                <img width="318" height="496" src="{{block.settings.banner | img_url: '318x496' }}" class="promo-banner-image image-1 attachment-full" alt="">
                              </div>
                            </div>
                            <div class="wrapper-content-banner">
                              <div class="content-banner text-left content-width-100 content-spacing-default">
                                <div class="banner-title-wrap banner-title-large">
                                  <h4 class="banner-title woodmart-font-weight-600"> 
                                    {% if settings.language_enable %} 
                                    <span class="lang1">{{ block.settings.banner_title | split: '|' | first }}</span>
                                    <span class="lang2">{{ block.settings.banner_title | split: '|' | last }}</span>
                                    {% else %}
                                    {{ block.settings.banner_title | split: '|' | first }}
                                    {% endif %}
                                  </h4>
                                </div>
                                <div class="banner-btn-wrapper">
                                  <div class="woodmart-button-wrapper text-left">
                                    <a href="{{block.settings.banner_link}}" title="" class="btn btn-color-black btn-style-default btn-size-small">
                                      {% if settings.language_enable %} 
                                      <span class="lang1">{{ block.settings.banner_link_text | split: '|' | first }}</span>
                                      <span class="lang2">{{ block.settings.banner_link_text | split: '|' | last }}</span>
                                      {% else %}
                                      {{ block.settings.banner_link_text | split: '|' | first }}
                                      {% endif %}
                                    </a>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          {%- endif -%}
        </div>
        <style type="text/css" data-type="vc_shortcodes-custom-css">.vc_custom_1501503456175{margin-right:-35px !important;margin-bottom:0px !important}.vc_custom_1491464148927{padding-top:0px !important}.vc_custom_1491464153829{padding-top:0px !important}.vc_custom_1501504499570{margin-top:-30px !important;padding-top:0px !important;padding-right:0px !important}.vc_custom_1491550062693{padding-left:0px !important}.vc_custom_1491550062693{padding-left:0px !important}.vc_custom_1491552748079{margin-bottom:-20px !important;padding-left:0px !important}.vc_custom_1491550062693{padding-left:0px !important}.vc_custom_1491550062693{padding-left:0px !important}.vc_custom_1491552752468{margin-bottom:-20px !important;padding-left:0px !important}.vc_custom_1491466272153{margin-right:-50px !important}.vc_custom_1491466426404{margin-bottom:-30px !important;padding-right:0px !important;padding-left:0px !important}</style>
      </div>
    </div>
    {%- endif -%} 
    <style type="text/css">
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}
      {%- endif -%} 
      .menu-item-{{block.id}} > .sub-menu-dropdown {
        min-height: 420px;
        {%- if block.settings.menu_width != blank and block.settings.full_width == blank -%} 
        width: {{ block.settings.menu_width | escape | default: '800' | remove: 'px' | append: 'px' }};
        {%- else -%}
        width: 100%;
        {%- endif -%} 
      }
    </style>
  </li>
  {%- when 'mega_sub_product1' -%} 
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-taxonomy menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-{% if block.settings.full_width != blank %}full-width{% else %}sized{% endif %} item-level-0 menu-mega-dropdown item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- assign child_list_handle = block.settings.menu -%}
    {%- if linklists[child_list_handle].links.size > 0 or linklists[child_list_handle].links != blank -%} 
    <div class="sub-menu-dropdown color-scheme-{{block.settings.sub_menu_color}}">
      <div class="container"> 
        <div class="vc_row wpb_row vc_row-fluid rtl-cat-margin vc_custom_1501502376704 vc_row-o-content-top vc_row-flex"> 
          {%- assign counts = linklists[child_list_handle].links.size | plus: 0 -%}  
          {%- case counts -%}
            {%- when 1 -%}  
              {%- assign class_menu = '6' -%} 
              {%- assign class_product = '6' -%} 
            {%- else -%}  
              {%- assign class_menu = '4' -%} 
              {%- assign class_product = '4' -%} 
          {%- endcase -%}
          {%- if counts > 0 -%}
          <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
            <div class="vc_column-inner">
              <div class="spb_wrapper"> 
                {%- for childlink in linklists[child_list_handle].links limit:1 -%}   
                	<ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                	</ul>  
                {%- endfor -%}
                {%- if counts > 2 -%}
                  {%- for childlink in linklists[child_list_handle].links offset:2 limit:1 -%}
                	<ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                	</ul> 
                  {%- endfor -%}
                {%- endif -%}
              </div>
            </div>
          </div> 
            {%- if counts > 1 -%}
            <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
              <div class="vc_column-inner">
                <div class="wpb_wrapper"> 
                  {%- for childlink in linklists[child_list_handle].links offset:1 limit:1 -%} 
                  	<ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                  </ul>  
                  {%- endfor -%}
                  {%- if counts > 3 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:3 limit:1 -%}  
                  	<ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                  	</ul>  
                    {%- endfor -%}
                  {%- endif -%} 
                </div>
              </div>
            </div> 
          	{%- endif -%}
          {%- endif -%} 
          <div class="rtl-cat-col-bord-right wpb_column vc_column_container vc_col-sm-{{class_product}} vc_col-has-fill">
            <div class="vc_column-inner vc_custom_1502111368761">
              <div class="wpb_wrapper">
                {%- assign limit = block.settings.products_list_limit -%}      
                {%- assign name_collection = block.settings.collection_products_list -%}
                {%- if name_collection == blank -%}{%- assign name_collection = "all" -%}{%- endif -%}
                {%- assign collection = collections[name_collection] -%} 
                {%- if collection.products.size == 0 -%}
                <p style="text-align:center;color:#999;padding: 5px 10px;font-style:italic;" {% if settings.language_enable %}data-translate="products.general.no_more_product"{% endif %}>{{ 'products.general.no_more_product' | t }}</p>
                {%- else -%} 
                <h5 style="font-size: 14px;line-height: 22px;text-align: left;text-transform:uppercase;" class="vc_custom_heading rtl-cat-right vc_custom_1501501649825">
                  {% if settings.language_enable %}
                  <span class="lang1">{{ collection.title | split: '|' | first }}</span>
                  <span class="lang2">{{ collection.title | split: '|' | last }}</span>
                  {% else %}
                  {{ collection.title | split: '|' | first }}
                  {% endif %}
                </h5>
                <div class="spb_raw_code spb_content_element spb_raw_html vc_custom_1506328342265">
                  <div class="wpb_wrapper">
                    <div class="widget shopify widget_products">  
                      <ul class="product_list_widget">
                        {%- for product in collection.products limit: limit -%} 
                        {%- assign on_sale = false -%}
                        {%- if product.compare_at_price_min > product.price_min -%}
                        {%- assign on_sale = true -%}
                        {%- endif -%} 
                        <li>
                          <a href="{{ product.url | within: collection }}">
                            <img src="{{ product.featured_image.src | img_url: '100x130' }}" class="attachment-shop_catalog size-shop_catalog" alt="">		
                            <span class="product-title">
                              {% if settings.language_enable %}
                              <span class="lang1">{{ product.title | split: '|' | first }}</span>
                              <span class="lang2">{{ product.title | split: '|' | last }}</span>
                              {% else %}
                              {{ product.title | split: '|' | first }}
                              {% endif %}
                            </span>
                          </a> 
                          <span class="price">
                            {%- if on_sale -%} 
                            <del><span class="shopify-Price-amount amount"><span class="money">{{ product.compare_at_price | money }}</span></span></del> 
                            <ins><span class="shopify-Price-amount amount"><span class="money">{{ product.price | money }}</span></span></ins>
                            {%- else -%}
                            <span class="shopify-Price-amount amount"><span class="money">{{ product.price | money }}</span></span>
                            {%- endif -%}
                          </span>
                        </li> 
                        {%- endfor -%} 
                      </ul>
                    </div> 
                  </div>
                </div> 
                {%- endif -%}
              </div>
            </div>
          </div>
        </div>
        <style type="text/css" data-type="vc_shortcodes-custom-css">.vc_custom_1501502376704{margin-right:-35px !important;margin-bottom:0px !important}.vc_custom_1502111368761{margin-top:-30px !important;border-left-width:1px !important;padding-top:30px !important;padding-right:30px !important;padding-bottom:43px !important;padding-left:30px !important;background-color:#f9f9f9 !important;border-left-color:#ededed !important;border-left-style:solid !important}.vc_custom_1501501649825{margin-bottom:12px !important}.vc_custom_1506328342265{margin-bottom:0px !important}</style>
      </div>
    </div>
    {%- endif -%} 
    <style type="text/css">
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}
      {%- endif -%} 
      .menu-item-{{block.id}} > .sub-menu-dropdown {
        min-height: 420px;
        {%- if block.settings.banner != blank and block.settings.show_img -%} 
        background-image: url({{ block.settings.banner| img_url: 'master' }});
        {%- endif -%}
        {%- if block.settings.menu_width != blank and block.settings.full_width == blank -%} 
        width: {{ block.settings.menu_width | escape | default: '800' | remove: 'px' | append: 'px' }};
        {%- else -%}
        width: 100%;
        {%- endif -%} 
      }
    </style>
  </li>
  {%- when 'mega_sub_product2' -%} 
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-taxonomy menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-{% if block.settings.full_width != blank %}full-width{% else %}sized{% endif %} item-level-0 menu-mega-dropdown item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- assign child_list_handle = block.settings.menu -%}
    {%- if linklists[child_list_handle].links.size > 0 or linklists[child_list_handle].links != blank -%} 
    <div class="sub-menu-dropdown color-scheme-{{block.settings.sub_menu_color}}">
      <div class="container"> 
        <div class="vc_row wpb_row vc_row-fluid vc_custom_1492520298430 vc_row-o-content-top vc_row-flex"> 
          {%- assign counts = linklists[child_list_handle].links.size | plus: 0 -%}  
          {%- assign class_parent = '12' -%}  
          {%- if block.settings.products_slide_use != blank -%} 
            {%- assign class_parent = '8' -%}  
            {%- assign class_product = '4' -%} 
          {%- endif-%}
          {%- case counts -%}
            {%- when 1 -%}  
              {%- assign class_menu = '12' -%}   
          	{%- when 2 -%}  
              {%- assign class_menu = '6' -%}  
            {%- else -%}  
              {%- assign class_menu = '4' -%}  
          {%- endcase -%}
          <div class="wpb_column vc_column_container vc_col-sm-{{ class_parent }}">
            {%- if counts > 0 -%}
            <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
              <div class="vc_column-inner">
                <div class="spb_wrapper"> 
                  {%- for childlink in linklists[child_list_handle].links limit:1 -%}   
                  <ul class="sub-menu mega-menu-list">
                    <li>
                      <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                        <span>
                        {% if settings.language_enable %} 
                        <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                        <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                        {% else %}
                        {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                        {% endif %}
                        </span>
                      </a>
                      {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                      {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                      <ul class="sub-sub-menu">
                        {%- if linklists[childlink.handle].links.size > 0 -%}
                        {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                        {%- elsif linklists[third_list_handle].links != blank -%}
                        {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                        {%- endif -%}
                        {%- for thirdlink in menu_sub_sub_link.links -%}
                        {%- if thirdlink.title contains '[' -%}
                        {%- if thirdlink.title contains '[icon_' -%}
                        {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <i class="fa fa-{{icon_third}}"></i> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- else -%}
                        {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        <li class="item-with-label item-label-{{label_third | handleize }}">
                          <a href="{{ thirdlink.url }}"> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                            <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- else -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- endfor -%}
                      </ul>
                      {%- endif -%}
                    </li>
                  </ul>  
                  {%- endfor -%}
                  {%- if counts > 3 -%}
                    {%- for childlink in linklists[child_list_handle].links offset:3 limit:1 -%}
                    <ul class="sub-menu mega-menu-list">
                    <li>
                      <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                        <span>
                        {% if settings.language_enable %} 
                        <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                        <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                        {% else %}
                        {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                        {% endif %}
                        </span>
                      </a>
                      {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                      {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                      <ul class="sub-sub-menu">
                        {%- if linklists[childlink.handle].links.size > 0 -%}
                        {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                        {%- elsif linklists[third_list_handle].links != blank -%}
                        {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                        {%- endif -%}
                        {%- for thirdlink in menu_sub_sub_link.links -%}
                        {%- if thirdlink.title contains '[' -%}
                        {%- if thirdlink.title contains '[icon_' -%}
                        {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <i class="fa fa-{{icon_third}}"></i> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- else -%}
                        {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                        <li class="item-with-label item-label-{{label_third | handleize }}">
                          <a href="{{ thirdlink.url }}"> 
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                            <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- else -%}
                        <li>
                          <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                            <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                            {% else %}
                            {{ thirdlink.title | split: '|' | first }}
                            {% endif %}
                            </span>
                          </a>
                        </li>
                        {%- endif -%}
                        {%- endfor -%}
                      </ul>
                      {%- endif -%}
                    </li>
                  </ul> 
                    {%- endfor -%}
                  {%- endif -%}
                </div>
              </div>
            </div> 
              {%- if counts > 1 -%}
              <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
                <div class="vc_column-inner">
                  <div class="wpb_wrapper"> 
                    {%- for childlink in linklists[child_list_handle].links offset:1 limit:1 -%} 
                    <ul class="sub-menu mega-menu-list">
                      <li>
                        <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                          <span>
                          {% if settings.language_enable %} 
                          <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                          <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                          {% else %}
                          {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                          {% endif %}
                          </span>
                        </a>
                        {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                        {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                        <ul class="sub-sub-menu">
                          {%- if linklists[childlink.handle].links.size > 0 -%}
                          {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                          {%- elsif linklists[third_list_handle].links != blank -%}
                          {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                          {%- endif -%}
                          {%- for thirdlink in menu_sub_sub_link.links -%}
                          {%- if thirdlink.title contains '[' -%}
                          {%- if thirdlink.title contains '[icon_' -%}
                          {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <i class="fa fa-{{icon_third}}"></i> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- else -%}
                          {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                          <li class="item-with-label item-label-{{label_third | handleize }}">
                            <a href="{{ thirdlink.url }}"> 
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                              <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- else -%}
                          <li>
                            <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                              <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                              {% else %}
                              {{ thirdlink.title | split: '|' | first }}
                              {% endif %}
                              </span>
                            </a>
                          </li>
                          {%- endif -%}
                          {%- endfor -%}
                        </ul>
                        {%- endif -%}
                      </li>
                    </ul>  
                    {%- endfor -%}
                    {%- if counts > 4 -%}
                      {%- for childlink in linklists[child_list_handle].links offset:4 limit:1 -%}  
                      <ul class="sub-menu mega-menu-list">
                        <li>
                          <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                          {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                          {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                          <ul class="sub-sub-menu">
                            {%- if linklists[childlink.handle].links.size > 0 -%}
                            {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                            {%- elsif linklists[third_list_handle].links != blank -%}
                            {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                            {%- endif -%}
                            {%- for thirdlink in menu_sub_sub_link.links -%}
                            {%- if thirdlink.title contains '[' -%}
                            {%- if thirdlink.title contains '[icon_' -%}
                            {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <i class="fa fa-{{icon_third}}"></i> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- else -%}
                            {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            <li class="item-with-label item-label-{{label_third | handleize }}">
                              <a href="{{ thirdlink.url }}">
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                                <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- else -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- endfor -%}
                          </ul>
                          {%- endif -%}
                        </li>
                      </ul>  
                      {%- endfor -%}
                    {%- endif -%} 
                  </div>
                </div>
              </div> 
              {%- endif -%}
              {%- if counts > 3 -%}
                <div class="wpb_column vc_column_container vc_col-sm-{{ class_menu }}">
                  <div class="vc_column-inner">
                    <div class="wpb_wrapper"> 
                      {%- for childlink in linklists[child_list_handle].links offset:2 limit:1 -%} 
                      <ul class="sub-menu mega-menu-list">
                        <li>
                          <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                            <span>
                            {% if settings.language_enable %} 
                            <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                            <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                            {% else %}
                            {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                            {% endif %}
                            </span>
                          </a>
                          {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                          {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                          <ul class="sub-sub-menu">
                            {%- if linklists[childlink.handle].links.size > 0 -%}
                            {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                            {%- elsif linklists[third_list_handle].links != blank -%}
                            {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                            {%- endif -%}
                            {%- for thirdlink in menu_sub_sub_link.links -%}
                            {%- if thirdlink.title contains '[' -%}
                            {%- if thirdlink.title contains '[icon_' -%}
                            {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <i class="fa fa-{{icon_third}}"></i> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- else -%}
                            {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                            <li class="item-with-label item-label-{{label_third | handleize }}">
                              <a href="{{ thirdlink.url }}"> 
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                {% endif %}
                                </span>
                                <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- else -%}
                            <li>
                              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                <span>
                                {% if settings.language_enable %} 
                                <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                {% else %}
                                {{ thirdlink.title | split: '|' | first }}
                                {% endif %}
                                </span>
                              </a>
                            </li>
                            {%- endif -%}
                            {%- endfor -%}
                          </ul>
                          {%- endif -%}
                        </li>
                      </ul>  
                      {%- endfor -%}
                      {%- if counts > 5 -%}
                        {%- for childlink in linklists[child_list_handle].links offset:5 limit:1 -%}  
                        <ul class="sub-menu mega-menu-list">
                          <li>
                            <a href="{{ childlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                              <span>
                              {% if settings.language_enable %} 
                              <span class="lang1">{{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                              <span class="lang2">{{ childlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                              {% else %}
                              {{ childlink.title | split: '|' | first | split: '[' | first | capitalize }}
                              {% endif %}
                              </span>
                            </a>
                            {%- assign third_list_handle = childlink.title | split: '[' | first | handleize -%}
                            {%- if linklists[childlink.handle].links.size > 0 or linklists[third_list_handle].links != blank -%}
                            <ul class="sub-sub-menu">
                              {%- if linklists[childlink.handle].links.size > 0 -%}
                              {%- assign menu_sub_sub_link  = linklists[childlink.handle] -%}
                              {%- elsif linklists[third_list_handle].links != blank -%}
                              {%- assign menu_sub_sub_link  = linklists[third_list_handle] -%}
                              {%- endif -%}
                              {%- for thirdlink in menu_sub_sub_link.links -%}
                              {%- if thirdlink.title contains '[' -%}
                              {%- if thirdlink.title contains '[icon_' -%}
                              {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <i class="fa fa-{{icon_third}}"></i> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- else -%}
                              {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
                              <li class="item-with-label item-label-{{label_third | handleize }}">
                                <a href="{{ thirdlink.url }}"> 
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                                  {% endif %}
                                  </span>
                                  <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- else -%}
                              <li>
                                <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                                  <span>
                                  {% if settings.language_enable %} 
                                  <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                                  <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                                  {% else %}
                                  {{ thirdlink.title | split: '|' | first }}
                                  {% endif %}
                                  </span>
                                </a>
                              </li>
                              {%- endif -%}
                              {%- endfor -%}
                            </ul>
                            {%- endif -%}
                          </li>
                        </ul>  
                        {%- endfor -%}
                      {%- endif -%} 
                    </div>
                  </div>
                </div> 
                {%- endif -%}
            {%- endif -%} 
          </div>
          {%- if block.settings.products_slide_use != blank -%}
          <div class="wpb_column vc_column_container vc_col-sm-{{class_product}}">
            <div class="vc_column-inner vc_custom_1491839677820"> 
              <div class="wpb_wrapper">
                {%- case block.settings.collection_hover_product -%}
                {%- when 'base' -%}
                {%- assign pl_include = 'section_hover_product_base' -%} 
                {%- when 'alt' -%}
                {%- assign pl_include = 'section_hover_product_alt' -%}
                {%- when 'info' -%}
                {%- assign pl_include = 'section_hover_product_info' -%}
                {%- when 'info-alt' -%}
                {%- assign pl_include = 'section_hover_product_info_alt' -%}
                {%- when 'button' -%}
                {%- assign pl_include = 'section_hover_product_button' -%}
                {%- when 'icon' -%}
                {%- assign pl_include = 'section_hover_product_icon' -%}
                {%- when 'standard' -%}
                {%- assign pl_include = 'section_hover_product_standard' -%} 
                {%- when 'quick' -%}
                {%- assign pl_include = 'section_hover_product_quick' -%}
                {%- endcase -%} 
                {%- assign limit = block.settings.products_list_limit -%}      
                {%- assign name_collection = block.settings.collection_products_list -%}
                {%- if name_collection == blank -%}{%- assign name_collection = "all" -%}{%- endif -%}
                {%- assign collection = collections[name_collection] -%} 
                {%- if collection.products.size == 0 -%}
                <p style="text-align:center;color:#999;padding: 5px 10px;font-style:italic;" {% if settings.language_enable %}data-translate="products.general.no_more_product"{% endif %}>{{ 'products.general.no_more_product' | t }}</p>
                {%- else -%} 
                <div id="carousel-{{block.id}}" class="vc_carousel_container">
                  <div class="data-carousel" data-auto="5000" data-items="1" data-1200="1" data-992="1" data-768="1" data-640="1" data-480="1" data-320="1" data-nav="true" data-prev='' data-next='' data-loop="true" data-paging="false" data-margin='0' style="display: none;"></div>
                  <div class="owl-items-xl-1 owl-items-lg-1 owl-items-md-1 owl-items-sm-1 owl-carousel carousel-init slider-type-product owl-loaded owl-drag"> 
                    {%- for product in collection.products limit: limit -%} 
                    {%- assign sold_out = true -%}
                    {%- if product.available -%}
                      {%- assign sold_out = false -%}
                    {%- endif -%}
                    {%- assign on_sale = false -%}
                    {%- if product.compare_at_price_min > product.price_min -%}
                      {%- assign on_sale = true -%}
                    {%- endif -%}
                    {%- assign variant_tmp = product.selected_or_first_available_variant -%}
                    {%- for variant in product.variants -%}
                      {%- if variant.available == true and variant.price < variant_tmp.price -%}
                          {%- assign variant_tmp = variant -%}	
                      {%- endif -%}
                    {%- endfor -%}
                    {%- case settings.collection_aspect_ratio -%}
                    {%- when '1' -%}
                      {%- assign image_width = settings.collection_ratio_width -%}
                      {%- assign image_height = '' -%} 
                    {%- else -%}
                      {%- assign image_width = settings.collection_ratio_width -%}
                      {%- assign image_height = settings.collection_ratio_height -%}
                    {%- endcase -%}  
                    {%- if settings.collection_different_grid == false -%}
                    {%- capture image_size -%}{{ image_width }}x{{ image_height }}{%- endcapture -%}
                    {%- endif -%} 
                    <div class="slide-product owl-carousel-item">
                      <div class="owl-carousel-item-inner">
                        {%- include pl_include, carousel: true, countdown: settings.collection_product_countdown -%}
                      </div>
                    </div> 
                    {%- endfor -%}  
                  </div> 
                </div> 
                {%- endif -%}
              </div>
            </div>
          </div> 
          {%- endif -%}
        </div>
        {%- assign _ch = 0 -%}
        {%- for i in (1..4) -%}
          {%- capture title -%}content_heading_{{i}}{%- endcapture -%} 
          {%- if block.settings[title] != '' -%}  
          	{%- assign _ch = _ch | plus: 1 -%}
          {%- endif -%}
        {%- endfor -%} 
        {%- if _ch > 0 -%}
        <div class="vc_row wpb_row vc_row-fluid rtl-cat-toys vc_custom_1501506244860 vc_row-has-fill vc_row-o-content-top vc_row-flex"> 
          {%- for i in (1..4) -%}
          {%- capture c_icon -%}content_icon_{{i}}{%- endcapture -%}
          {%- capture c_title -%}content_heading_{{i}}{%- endcapture -%}
          {%- capture c_text -%}content_text_{{i}}{%- endcapture -%}
          {%- if block.settings[c_title] != '' -%}
          <div class="rtl-toys-column wpb_column vc_column_container vc_col-sm-{{ 12 | divided_by: _ch }}">
            <div class="vc_column-inner vc_custom_1501506757584">
              <div class="wpb_wrapper">
                <div class="wpb_text_column wpb_content_element {% if forloop.index == _ch %}no-border{%else%}vc_custom_1491892802508 has-border{%endif%}">
                  <div class="wpb_wrapper">
                    <h6 style="text-transform: uppercase; margin-bottom: 5px; font-size: 14px;">
                      <strong>
                        {%- if block.settings[c_icon] != '' -%}
                        <i class="fa fa-{{block.settings[c_icon]}}" style="margin-right: 7px;"></i>
                        {%- endif -%}
                        {% if settings.language_enable %}
                        <span class="lang1">{{ block.settings[c_title] | split: '|' | first }}</span>
                        <span class="lang2">{{ block.settings[c_title] | split: '|' | last }}</span>
                        {% else %}
                        {{ block.settings[c_title] | split: '|' | first }}
                        {% endif %}
                      </strong>
                    </h6>
                    <p>
                      {% if settings.language_enable %}
                      <span class="lang1">{{ block.settings[c_text] | split: '|' | first }}</span>
                      <span class="lang2">{{ block.settings[c_text] | split: '|' | last }}</span>
                      {% else %}
                      {{ block.settings[c_text] | split: '|' | first }}
                      {% endif %}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
          {%- endif -%}
          {%- endfor -%}
        </div>
        <style type="text/css" data-type="vc_shortcodes-custom-css">.vc_custom_1492520298430{margin-bottom:0px !important}.vc_custom_1501506244860{margin-top:-8px !important;margin-right:-35px !important;margin-left:-35px !important;padding-top:18px !important;padding-right:35px !important;padding-bottom:18px !important;padding-left:20px !important;background-color:#f9f9f9 !important}.vc_custom_1491839677820{padding-right:20px !important;padding-left:20px !important}.vc_custom_1491565896651{margin-bottom:-20px !important;padding-top:0px !important}.vc_custom_1491565899851{margin-bottom:-20px !important;padding-top:0px !important}.vc_custom_1501506757584{padding-top:0px !important}.vc_custom_1501506740069{padding-top:0px !important}.vc_custom_1501506537503{padding-top:0px !important}.vc_custom_1501507120680{padding-top:0px !important}.vc_custom_1491892802508{margin-bottom:0px !important;border-right-width:1px !important;border-right-color:#e0e0e0 !important;border-right-style:solid !important}.vc_custom_1491892860819{margin-bottom:0px !important;border-right-width:1px !important;border-right-color:#e0e0e0 !important;border-right-style:solid !important}.vc_custom_1491892716385{margin-bottom:0px !important;border-right-width:1px !important;border-right-color:#e0e0e0 !important;border-right-style:solid !important}.vc_custom_1501507042598{margin-bottom:0px !important}</style>
        {%- endif -%}
      </div>
    </div>
    {%- endif -%} 
    <style type="text/css">
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}
      {%- endif -%} 
      .menu-item-{{block.id}} > .sub-menu-dropdown {
        min-height: 420px;
        {%- if block.settings.banner != blank and block.settings.show_img -%} 
        background-image: url({{ block.settings.banner| img_url: 'master' }});
        {%- endif -%}
        {%- if block.settings.menu_width != blank and block.settings.full_width == blank -%} 
        width: {{ block.settings.menu_width | escape | default: '800' | remove: 'px' | append: 'px' }};
        {%- else -%}
        width: 100%;
        {%- endif -%} 
      }
    </style>
  </li>
  {%- when 'classic_dropdown' -%}
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-post_type menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-default item-level-0 item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- assign child_list_handle = block.settings.menu -%}
    {%- if linklists[child_list_handle].links != blank -%}
    <div class="sub-menu-dropdown color-scheme-dark">
      <div class="container">
        <ul class="sub-menu color-scheme-dark">
          {%- for thirdlink in linklists[child_list_handle].links -%}
          {%- if thirdlink.title contains '[' -%}
            {%- if thirdlink.title contains '[icon_' -%}
              {%- assign _icon_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
              {%- assign icon_third  = _icon_third |  split: 'icon_' | last -%}
                <li>
                  <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                    <i class="fa fa-{{icon_third}}"></i> 
                    <span>
                    {% if settings.language_enable %} 
                    <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                    <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                    {% else %}
                    {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                    {% endif %}
                    </span>
                  </a>
          		</li>
            {%- else -%}
              {%- assign label_third  = thirdlink.title |  split: '[' | last  | split: ']' | first -%}
              <li class="item-with-label item-label-{{label_third | handleize }}">
                <a href="{{ thirdlink.url }}">
                  <span>
                  {% if settings.language_enable %} 
                  <span class="lang1">{{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}</span>
                  <span class="lang2">{{ thirdlink.title | split: '|' | last | split: '[' | first | capitalize }}</span>
                  {% else %}
                  {{ thirdlink.title | split: '|' | first | split: '[' | first | capitalize }}
                  {% endif %}
                  </span>
                  <span class="menu-label menu-label-{{label_third | handleize }}">{{label_third | upcase }}</span>
                </a>
              </li>
            {%- endif -%}
          {%- else -%}
            <li>
              <a href="{{ thirdlink.url }}"{%- if childlink.active %} class="current-menu-item"{%- endif -%}>
                <span>
                {% if settings.language_enable %} 
                <span class="lang1">{{ thirdlink.title | split: '|' | first }}</span>
                <span class="lang2">{{ thirdlink.title | split: '|' | last }}</span>
                {% else %}
                {{ thirdlink.title | split: '|' | first }}
                {% endif %}
                </span>
              </a>
            </li>
          {%- endif -%}
          {%- endfor -%}
        </ul>
      </div>
    </div>
    {%- endif -%}
  </li>
  <style type="text/css">
    .menu-item-{{block.id}} > .sub-menu-dropdown {width: {{ block.settings.menu_width | escape | default: '250' | remove: 'px' | append: 'px' }}; }
  	{%- if block.settings.label != blank and block.settings.color != blank -%}.menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}.menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}{%- endif -%}
  </style>
  {%- when 'custom_html_block' -%}
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-post_type menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} menu-item-design-{% if block.settings.full_width != blank %}full-width{% else %}sized{% endif %} item-level-0 menu-mega-dropdown item-event-{{section.settings.action_sub_menu}} menu-item-has-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    {%- if block.settings.html != blank -%}
    <div class="sub-menu-dropdown color-scheme-dark">
      <div class="container"> 
        {% if settings.language_enable %}
        <div class="lang1">{{ block.settings.html | split: '|' | first }}</div>
        <div class="lang2">{{ block.settings.html | split: '|' | last }}</div>
        {% else %}
        {{ block.settings.html | split: '|' | first }}
        {% endif %}
      </div>
    </div>
    {%- endif -%}
    <style type="text/css">
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}
      .menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}
      {%- endif -%} 
      .menu-item-{{block.id}} > .sub-menu-dropdown {
        min-height: 120px;
        {%- if block.settings.banner != blank and block.settings.show_img -%} 
        background-image: url({{ block.settings.banner| img_url }});
        {%- endif -%}
        {%- if block.settings.menu_width != blank -%} 
        width: {{ block.settings.menu_width | escape | default: '800' | remove: 'px' | append: 'px' }};
        {%- endif -%} 
      }
    </style>
  </li>
  {%- else -%}
  <li id="menu-item-{{block.id}}" class="menu-item menu-item-type-custom menu-item-{{block.settings.title | handleize}} menu-item-{{block.id}} item-level-0 menu-item-no-children with-offsets">
    <a href="{{block.settings.button_link}}" class="woodmart-nav-link">
      {%- if block.settings.icon_svg != '' -%}
      <div class="category-icon" style="width: 19px;height: 19px;">{{block.settings.icon_svg}}</div>
      {%- else -%}
      {%- if block.settings.icon != blank -%}<div class="category-icon"><i class="fa fa-{{block.settings.icon}}"></i></div>{%- endif -%}
      {%- endif -%}
      <span>
        {% if settings.language_enable %}
        <span class="lang1">{{ block.settings.title | split: '|' | first }}</span>
        <span class="lang2">{{ block.settings.title | split: '|' | last }}</span>
        {% else %}
        {{ block.settings.title | split: '|' | first }}
        {% endif %}
      </span>
      {%- if block.settings.label != blank and block.settings.color != blank -%}
      <span class="menu-label menu-label-{{block.settings.label | handleize}}">{{block.settings.label}}</span>
      {%- endif -%}
    </a>
    <style type="text/css">{%- if block.settings.label != blank and block.settings.color != blank -%}.menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}{ background-color: {{block.settings.color}}}.menu-item-{{block.id}} .menu-label-{{block.settings.label | handleize}}:before{ border-color: {{block.settings.color}}}{%- endif -%}</style>
  </li>
  {%- endcase -%}
  {%- endfor -%}
</ul>
{%- if section.settings.label_1 != blank or section.settings.label_2 != blank or section.settings.label_3 != blank -%}
<style type="text/css">
  {%- capture label_css -%}
  {%- for i in (1..3) -%}
  {%- assign label = 'label_' | append:i -%}
  {%- assign color = 'color_' | append:i -%}
  {%- assign label_id = section.settings[label] -%}
  {%- assign color_id = section.settings[color] -%} 
  {%- if label_id != blank and color_id != blank -%}
  .menu-categories-container .menu-label-{{label_id | handleize }} {background-color: {{color_id}}}.menu-categories-container .menu-label-{{label_id | handleize }}:after { color: {{color_id}} }
  {%- endif -%}
  {%- endfor -%}
  {%- endcapture -%}
  {{label_css}}
</style>
{%- endif -%}
{%- endif -%}
{%- schema -%}
{
  "name": "Categories Menu", 
  "settings": [
    {
      "type": "radio",
      "id": "action_sub_menu",
      "label": "Action Show Sub Menu",
      "options": [
        { 
          "value": "hover", 
          "label": "Hover" 
		},
        { 
          "value": "click",
          "label": "Click" 
		}
      ],
      "default":   "hover"
    },
    {
      "type": "header",
      "content": "Label menu Settings"
    },
    {
      "type":"text",
      "id":"label_1",
      "label":"Menu label 1"
    },
    {
      "type":"color",
      "id":"color_1",
      "label":"Label color 1"
    },
    {
      "type":"text",
      "id":"label_2",
      "label":"Menu label 2"
    },
    {
      "type":"color",
      "id":"color_2",
      "label":"Label color 2"
    },
    {
      "type":"text",
      "id":"label_3",
      "label":"Menu label 3"
    },
    {
      "type":"color",
      "id":"color_3",
      "label":"Label color 3"
    }
  ],
  "blocks": [
    {
      "type":"mega_dropdown",
      "name":"Mega Dropdown",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Add sub menu"
        }, 
        {
          "type": "select",
          "id": "sub_menu_color",
          "label": "Sub Menu Text Color",
          "options": [
            { 
              "value": "light", 
              "label": "Light" 
            },
            { 
              "value": "dark",
              "label": "Dark" 
            }
          ],
          "default":   "dark"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        },
        {
          "type": "checkbox",
          "id": "show_img",
          "label": "Show image?"
        },
        {
          "type": "image_picker",
          "id": "banner",
          "label": "Banner image"
        }, 
        {
          "type": "html",
          "label": "Banner Custom Style",
          "id": "banner_custom_style", 
          "info": "Custom position banner"
        },
        {
          "type": "checkbox",
          "id": "full_width",
          "label": "Show Sub full-width?"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "default": "800",
          "info": "Set width sub megamenu."
        },
        {
          "type": "header", 
          "content": "==Banner image sub top menu"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top1",
          "label": "Banner image sub 1"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top2",
          "label": "Banner image sub 2"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top3",
          "label": "Banner image sub 3"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top4",
          "label": "Banner image sub 4"
        }, 
        {
          "type": "image_picker",
          "id": "banner_sub_top5",
          "label": "Banner image sub 5"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top6",
          "label": "Banner image sub 6"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top7",
          "label": "Banner image sub 7"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top8",
          "label": "Banner image sub 8"
        },
        {
          "type": "header", 
          "content": "==Brand banner image"
        },
        {
          "type": "image_picker",
          "id": "brand_image_1",
          "label": "Banner image 1"
        },
        {
          "type": "url",
          "id": "brand_link_1",
          "label": "Brand link 1"
        },
        {
          "type": "image_picker",
          "id": "brand_image_2",
          "label": "Banner image 2"
        },
        {
          "type": "url",
          "id": "brand_link_2",
          "label": "Brand link 2"
        },
        {
          "type": "image_picker",
          "id": "brand_image_3",
          "label": "Banner image 3"
        },
        {
          "type": "url",
          "id": "brand_link_3",
          "label": "Brand link 3"
        },
        {
          "type": "image_picker",
          "id": "brand_image_4",
          "label": "Banner image 4"
        },
        {
          "type": "url",
          "id": "brand_link_4",
          "label": "Brand link 4"
        }
      ]
    },
    {
      "type":"mega_sub_banner_content",
      "name":"Mega Sub Banner Content",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Add sub menu"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        }, 
        {
          "type": "checkbox",
          "id": "full_width",
          "label": "Show Sub full-width?"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "default": "800",
          "info": "Set width sub megamenu."
        },
        {
          "type": "header", 
          "content": "==Banner Content Image"
        }, 
        {
          "type": "image_picker",
          "id": "banner",
          "label": "Banner image"
        },
        {
          "type": "text",
          "id": "banner_title",
          "label": "Banner Title"
        },
        {
          "type": "url",
          "id": "banner_link",
          "label": "Banner Link"
        }, 
        {
          "type": "text",
          "id": "banner_link_text",
          "label": "Banner Link Text",
		  "default": "Shop Now"
        },
        {
          "type": "header", 
          "content": "==Banner image sub top menu"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top1",
          "label": "Banner image sub 1"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top2",
          "label": "Banner image sub 2"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top3",
          "label": "Banner image sub 3"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top4",
          "label": "Banner image sub 4"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top5",
          "label": "Banner image sub 5"
        },
        {
          "type": "image_picker",
          "id": "banner_sub_top6",
          "label": "Banner image sub 6"
        }
      ]
    },
    {
      "type":"mega_sub_product1",
      "name":"Mega Sub Product List",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Add sub menu"
        },
        {
          "type": "select",
          "id": "sub_menu_color",
          "label": "Sub Menu Text Color",
          "options": [
            { 
              "value": "light", 
              "label": "Light" 
            },
            { 
              "value": "dark",
              "label": "Dark" 
            }
          ],
          "default":   "dark"
        },
        {
          "type": "checkbox",
          "id": "show_img",
          "label": "Show image?"
        },
        {
          "type": "image_picker",
          "id": "banner",
          "label": "Banner image"
        },
        {
          "type": "html",
          "label": "Banner Custom Style",
          "id": "banner_custom_style", 
          "info": "Custom position banner"
        },
        {
          "type": "checkbox",
          "id": "full_width",
          "label": "Show sub full-width?"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "default": "800",
          "info": "Set width sub megamenu."
        }, 
        {
          "type": "header",
          "content": "Products List Settings"
        },
        {
          "type": "collection",
          "label": "Collection Products",
          "id": "collection_products_list"
        },
        {
          "type": "text",
          "id": "products_list_limit",
          "label": "Limit",
          "default": "4"
        }
      ]
    },
    {
      "type":"mega_sub_product2",
      "name":"Mega Sub Products Slide",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Add sub menu"
        },
        {
          "type": "select",
          "id": "sub_menu_color",
          "label": "Sub Menu Text Color",
          "options": [
            { 
              "value": "light", 
              "label": "Light" 
            },
            { 
              "value": "dark",
              "label": "Dark" 
            }
          ],
          "default":   "dark"
        },
        {
          "type": "checkbox",
          "id": "show_img",
          "label": "Show image?"
        },
        {
          "type": "image_picker",
          "id": "banner",
          "label": "Banner image"
        },
        {
          "type": "html",
          "label": "Banner Custom Style",
          "id": "banner_custom_style", 
          "info": "Custom position banner"
        },
        {
          "type": "checkbox",
          "id": "full_width",
          "label": "Show sub full-width?"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "default": "800",
          "info": "Set width sub megamenu."
        }, 
        {
          "type": "header",
          "content": "==Products Settings"
        },  
        {
          "type": "checkbox",
          "id": "products_slide_use",
          "label": "Show products slide?",
          "default": true
        }, 
        {
          "type": "collection",
          "label": "Collection Products",
          "id": "collection_products_slide"
        }, 
        {
          "type": "text",
          "id": "products_list_limit",
          "label": "Limit",
          "default": "4"
        }, 
        {
          "type": "select",
          "id": "collection_hover_product",
          "label": "Hover On Product",
          "info": "Choose one of those hover effects for products",
          "options": [
            {
              "value": "base",
              "label": "Base"
            },
            {
              "value": "alt",
              "label": "Alternative"
            },
            {
              "value": "info",
              "label": "Full info on image"
            },
            {
              "value": "info-alt",
              "label": "All info on hover"
            },
            {
              "value": "button",
              "label": "Button on image"
            },
            {
              "value": "icon",
              "label": "Icon on image"
            },
            {
              "value": "standard",
              "label": "Standard button"
            },
            {
              "value": "quick",
              "label": "Quick shop"
            }
          ],
          "default": "base"
        }, 
        {
          "type": "checkbox",
          "id": "products_count_down_use",
          "label": "Use sale count down",
          "default": true
        }, 
        {
          "type": "select",
          "id": "products_countdown_style",
          "label": "Countdown Style",
          "options": [
            {
              "value": "light",
              "label": "Light"
            },
            {
              "value": "dark",
              "label": "Dark"
            } 
          ],
          "default": "dark"
        }, 
        {
          "type": "header",
          "content": "==Custom Content Settings"
        }, 
        {
          "type": "header",
          "content": "Custom Content 1"
        },
        {
          "type": "text",
          "id": "content_icon_1",
          "label": "Icon",
          "default": "plane"
        },
        {
          "type": "text",
          "id": "content_heading_1",
          "label": "Heading",
          "default": "UPS SERVICE"
        },
        {
          "type": "text",
          "id": "content_text_1",
          "label": "Text",
          "default": "Shipping & Returns"
        },
        {
          "type": "header",
          "content": "Custom Content 2"
        },
        {
          "type": "text",
          "id": "content_icon_2",
          "label": "Icon",
          "default": "shopping-bag"
        },
        {
          "type": "text",
          "id": "content_heading_2",
          "label": "Heading",
          "default": "STORES"
        },
        {
          "type": "text",
          "id": "content_text_2",
          "label": "Text",
          "default": "Find retail locations"
        },
        {
          "type": "header",
          "content": "Custom Content 3"
        },
        {
          "type": "text",
          "id": "content_icon_3",
          "label": "Icon",
          "default": "truck"
        },
        {
          "type": "text",
          "id": "content_heading_3",
          "label": "Heading",
          "default": "FREE SHIPPING"
        },
        {
          "type": "text",
          "id": "content_text_3",
          "label": "Text",
          "default": "For orders above €100"
        },
        {
          "type": "header",
          "content": "Custom Content 4"
        },
        {
          "type": "text",
          "id": "content_icon_4",
          "label": "Icon",
          "default": "info-circle"
        },
        {
          "type": "text",
          "id": "content_heading_4",
          "label": "Heading",
          "default": "CUSTOMER CARE"
        },
        {
          "type": "text",
          "id": "content_text_4",
          "label": "Text",
          "default": "For All Your Questions"
        }
      ]
    },
    {
      "type":"classic_dropdown",
      "name":"Classic Menu Dropdown",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Add sub menu"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "info": "Pixcel unit, Example: 250 (if you not use full-width )"
        }
      ]
    },
    {
      "type":"custom_html_block",
      "name":"HTML block",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        },
        {
          "type": "html",
          "id": "html",
          "label": "Custom HTMl",
          "info": "Create a drop-down menu"
        },
        {
          "type": "text",
          "label": "Navigation width",
          "id": "menu_width",
          "default": "800",
          "info": "Pixcel unit, Example: 750 (if you not use full-width )"
        }
      ]
    },
    {
      "type":"menu_item_no_children",
      "name":"Menu item no children",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Navigation Title",
          "default": "Home",
          "info": "Ex: Home"
        },
        {
          "type": "textarea",
          "id": "icon_svg",
          "label": "Icon SVG",
          "info":"Use icon svg code"
        },
        {
          "id": "icon",
          "type": "text",
          "label": "Icon",
          "info":"Ex:user [fontawesome icons](http://fontawesome.io/icons/)"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Navigation link"
        },
        {
          "type":"text",
          "id":"label",
          "label":"Navigation label"
        },
        {
          "type":"color",
          "id":"color",
          "label":"Navigation label color",
          "default":"#ffffff"
        }
      ]
    }
  ]
}
{% endschema %}