
            {%- for block in section.blocks -%}
            <div class="brand-item">
                {%- if block.settings.image != blank -%}
                {%- assign img_url = block.settings.image | img_url: '1024x400' -%}
                {%- else -%}
                {%- assign img_url = '1024x400.png' | asset_url -%}
                {%- endif -%}
                <img src="{{ img_url }}" alt="{{ block.settings.image.alt }}"/>
              <p>{{ block.settings.text }}</p>
            </div>
            {%- endfor -%}
         
{% schema %}
{
  "name": "product shipping",
  "settings": [    
    
  ],
  "blocks": [
    {
      "type": "slider_banner",
      "name": "sider banner Image",
      "settings": [ 
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
       
		{
          "type": "text",
          "id": "text",
          "label": "Text"
        }
      ]
    } 
  ]
}
{% endschema %}